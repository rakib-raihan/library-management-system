import React, { Component } from "react";
import PageTitle from "../../../module/PageTitle";

class Profile extends Component {
  render() {
    return <React.Fragment>{PageTitle("Profile")}</React.Fragment>;
  }
}

export default Profile;
